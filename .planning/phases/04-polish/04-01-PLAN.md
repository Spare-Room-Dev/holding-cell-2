---
phase: 04-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/globals.css
  - frontend/src/components/ThemeToggle.tsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Light mode colors appear when toggle is activated"
    - "Dark mode is default on page load"
    - "Toggle switches between dark and light modes"
  artifacts:
    - path: "frontend/src/app/globals.css"
      provides: "Light mode CSS custom properties"
      contains: ":root.light"
    - path: "frontend/src/components/ThemeToggle.tsx"
      provides: "Sun/moon toggle button component"
      exports: ["ThemeToggle"]
  key_links:
    - from: "frontend/src/components/ThemeToggle.tsx"
      to: "document.documentElement.classList"
      via: "useEffect toggle"
      pattern: "classList.toggle\\('light'"
---

<objective>
Implement light/dark mode theme toggle with CSS custom properties. Dark mode is the default; light mode is activated via a toggle button in the header. No localStorage persistence per D-12.

Purpose: Enable recruiters to view the dashboard in light mode for different lighting conditions.
Output: Light mode CSS tokens and ThemeToggle component.
</objective>

<execution_context>
@/Users/rob/Documents/OT Apps/Holding Cell 2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rob/Documents/OT Apps/Holding Cell 2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish/04-CONTEXT.md
@.planning/phases/04-polish/04-RESEARCH.md
@.planning/phases/04-polish/04-UI-SPEC.md
@DESIGN.md

<interfaces>
<!-- From existing layout.tsx -->
```typescript
// Dark mode class already set on <html>
<html lang="en" className={`dark ${dmSans.variable} ${ibmPlexMono.variable}`}>
```

<!-- From existing globals.css -->
```css
:root {
  --color-background: #0D0D0D;
  --color-surface: #1A1A1A;
  --color-surface-raised: #222222;
  --color-border: #2A2A2A;
  --color-phosphor: #00FF88;
  --color-text-primary: #F0F0F0;
  /* ... etc */
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add light mode CSS custom properties</name>
  <files>frontend/src/app/globals.css</files>
  <read_first>
    - frontend/src/app/globals.css (see existing :root block structure)
    - .planning/phases/04-polish/04-UI-SPEC.md (exact color values required)
  </read_first>
  <action>Add a `:root.light` selector block after the existing `:root` block (around line 40) with light mode color tokens. Per D-11, use these exact values:
- Background: `#F5F4F0`
- Surface: `#FFFFFF`
- Surface Raised: `#FAFAFA`
- Border: `#E0DDD5`
- Border Subtle: `#ECEAE5`
- Phosphor: `#00DB75` (desaturated 15%)
- Phosphor Dim: `#00B05A`
- Phosphor Glow: `rgba(0, 219, 117, 0.15)`
- Amber: `#E5A600` (muted)
- Alert: `#E53550` (muted)
- Text Primary: `#1A1A1A`
- Text Muted: `#6B6B6B`
- Text Subtle: `#999999`

Also add `color-scheme: light;` in the `:root.light` block for proper form control styling. The dark mode class `html.dark` should also set `color-scheme: dark;`.

Do NOT modify the existing `:root` dark mode tokens — they are already correct.
</action>
  <verify>
    <automated>grep -q ":root.light" frontend/src/app/globals.css && grep -q "color-scheme: light" frontend/src/app/globals.css</automated>
  </verify>
  <done>
    - `:root.light` selector exists in globals.css with all light mode color tokens
    - `color-scheme: light` property set for proper browser theming
    - `html.dark` rule added for `color-scheme: dark`
    - Existing dark mode `:root` tokens unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ThemeToggle component</name>
  <files>frontend/src/components/ThemeToggle.tsx</files>
  <read_first>
    - frontend/src/app/layout.tsx (understand dark class setup)
    - frontend/src/components/ConnectionStatus.tsx (reference for component structure)
    - .planning/phases/04-polish/04-UI-SPEC.md (sun/moon icon specifications)
  </read_first>
  <action>Create a new ThemeToggle component at `frontend/src/components/ThemeToggle.tsx` with:

1. 'use client' directive (React state needed)
2. useState with `true` as default (dark mode is default per D-16)
3. useEffect that toggles `.light` class on `document.documentElement` and ensures `.dark` class is set for dark mode
4. NO localStorage persistence per D-12
5. Sun icon (for switching TO light mode) and moon icon (for switching TO dark mode) as inline SVG
6. Button styling: `p-sm rounded-md hover:bg-surface-raised transition-colors`
7. aria-label for accessibility: "Switch to light mode" / "Switch to dark mode"

The component toggles between dark (default) and light by adding/removing the `.light` class alongside the existing `.dark` class on the `<html>` element.

Icon paths (inline SVG):
- Sun icon: `<path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" />`
- Moon icon: `<path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />`

Export as named export `ThemeToggle`.
</action>
  <verify>
    <automated>grep -q "export function ThemeToggle" frontend/src/components/ThemeToggle.tsx && grep -q "classList.toggle('light'" frontend/src/components/ThemeToggle.tsx && grep -q "aria-label=" frontend/src/components/ThemeToggle.tsx</automated>
  </verify>
  <done>
    - ThemeToggle.tsx exists with 'use client' directive
    - useState initialized to `true` (dark mode default)
    - useEffect toggles .light class on document.documentElement
    - No localStorage usage
    - Sun/moon icons rendered conditionally
    - Named export ThemeToggle available
  </done>
</task>

</tasks>

<verification>
- `grep ":root.light" frontend/src/app/globals.css` returns light mode selector
- `grep "color-scheme: light" frontend/src/app/globals.css` returns color-scheme for light mode
- `grep "export function ThemeToggle" frontend/src/components/ThemeToggle.tsx` returns component export
- `grep "classList.toggle" frontend/src/components/ThemeToggle.tsx` returns DOM manipulation
</verification>

<success_criteria>
1. Light mode CSS custom properties defined with all colors from UI-SPEC.md
2. ThemeToggle component created with dark mode default
3. No localStorage persistence (class toggles on each visit)
4. Sun/moon icons switch based on current mode
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish/04-01-SUMMARY.md`
</output>