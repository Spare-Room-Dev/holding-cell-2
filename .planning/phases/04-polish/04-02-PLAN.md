---
phase: 04-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/BottomSheet.tsx
  - frontend/src/app/page.tsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Desktop (>=1024px) shows 70/30 sidebar layout unchanged"
    - "Tablet (768-1023px) shows stacked layout with full-width stats"
    - "Mobile (<768px) hides sidebar stats and shows stats button in header"
    - "Mobile stats button opens bottom sheet with slide-up animation"
    - "Tapping backdrop or swiping down dismisses bottom sheet"
  artifacts:
    - path: "frontend/src/components/BottomSheet.tsx"
      provides: "Mobile bottom sheet with Framer Motion animation"
      exports: ["BottomSheet"]
    - path: "frontend/src/app/page.tsx"
      provides: "Responsive layout with breakpoint classes"
      contains: "lg:flex-row"
  key_links:
    - from: "frontend/src/app/page.tsx"
      to: "frontend/src/components/BottomSheet.tsx"
      via: "import and use"
      pattern: "import.*BottomSheet"
    - from: "frontend/src/components/BottomSheet.tsx"
      to: "framer-motion"
      via: "AnimatePresence, motion"
      pattern: "from 'framer-motion'"
---

<objective>
Implement responsive layout with Tailwind breakpoints and mobile bottom sheet for stats panel. Desktop (>=1024px) keeps 70/30 sidebar. Tablet (768-1023px) stacks stats below JailCellGrid. Mobile (<768px) hides stats sidebar and shows bottom sheet on button tap.

Purpose: Enable dashboard use on tablets and phones without layout breaks.
Output: BottomSheet component and responsive page.tsx layout.
</objective>

<execution_context>
@/Users/rob/Documents/OT Apps/Holding Cell 2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rob/Documents/OT Apps/Holding Cell 2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish/04-CONTEXT.md
@.planning/phases/04-polish/04-RESEARCH.md
@.planning/phases/04-polish/04-UI-SPEC.md
@DESIGN.md

<interfaces>
<!-- From existing page.tsx -->
```tsx
// Current 70/30 layout (desktop only)
<div className="flex flex-row gap-md h-full">
  <div className="flex-[7] h-full">
    <JailCellGrid />
  </div>
  <div className="flex-[3] h-full overflow-y-auto bg-surface rounded-lg border border-border p-md">
    <StatsPanel />
  </div>
</div>
```

<!-- From existing StatsPanel.tsx -->
```tsx
// StatsPanel is a simple flex row with flex-wrap
<div className="flex flex-row gap-md flex-wrap">
  <CounterBox label="Total" value={counts.total} />
  {/* ... archetype counters */}
</div>
```

<!-- From DESIGN.md motion timing -->
Medium transitions: 250-400ms (for bottom sheet)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BottomSheet component</name>
  <files>frontend/src/components/BottomSheet.tsx</files>
  <read_first>
    - .planning/phases/04-polish/04-RESEARCH.md (Pattern 2: Bottom Sheet with Framer Motion)
    - DESIGN.md (motion timing: 250-400ms for medium transitions)
  </read_first>
  <action>Create `frontend/src/components/BottomSheet.tsx` with Framer Motion AnimatePresence for slide-up animation.

Per D-07 and D-08:
- Backdrop: semi-transparent black overlay (`bg-black/50`) that dismisses on tap
- Sheet: slides up from bottom with spring animation (stiffness: 300, damping: 30)
- Drag gesture: swipe down dismisses sheet (check offset.y > 100 or velocity.y > 500)
- Max height: `max-h-[70vh]` to prevent full screen coverage
- Rounded top corners: `rounded-t-lg`
- Drag handle: visual indicator at top (`w-12 h-1 bg-text-subtle rounded-full mx-auto mb-md`)

Props interface:
```typescript
interface BottomSheetProps {
  isOpen: boolean;
  onClose: () => void;
  children: React.ReactNode;
}
```

Animation values (per DESIGN.md Medium timing: 250-400ms):
- Backdrop: opacity 0 → 1 with duration 0.25s
- Sheet: y: '100%' → 0 with spring type

Use `AnimatePresence` with conditional render. Include accessibility attributes: `role="dialog"` and `aria-modal="true"` on the sheet container.

Export as named export `BottomSheet`.
</action>
  <verify>
    <automated>grep -q "export function BottomSheet" frontend/src/components/BottomSheet.tsx && grep -q "AnimatePresence" frontend/src/components/BottomSheet.tsx && grep -q "drag=" frontend/src/components/BottomSheet.tsx</automated>
  </verify>
  <done>
    - BottomSheet.tsx exists with AnimatePresence for enter/exit animations
    - Backdrop fades in/out with 250ms duration
    - Sheet slides up/down with spring physics
    - Drag gesture support for swipe-to-dismiss
    - Accessibility attributes (role, aria-modal) included
    - Named export BottomSheet available
  </done>
</task>

<task type="auto">
  <name>Task 2: Make page.tsx responsive with mobile stats button</name>
  <files>frontend/src/app/page.tsx</files>
  <read_first>
    - frontend/src/app/page.tsx (current 70/30 layout structure)
    - .planning/phases/04-polish/04-CONTEXT.md (D-01 through D-08 breakpoint decisions)
    - .planning/phases/04-polish/04-UI-SPEC.md (responsive breakpoint specifications)
  </read_first>
  <action>Update `frontend/src/app/page.tsx` to implement responsive layout per CONTEXT.md D-01 through D-04:

1. Add useState for `isStatsOpen` to control bottom sheet visibility
2. Import `BottomSheet` from '@/components/BottomSheet' and `ThemeToggle` from '@/components/ThemeToggle'
3. Update header to include:
   - ThemeToggle component next to ConnectionStatus
   - "Stats" button visible only on mobile/tablet (`className="lg:hidden"`)
   - Stats button triggers `setIsStatsOpen(true)`

4. Update main content layout:
   - Container: `flex flex-col lg:flex-row gap-md h-full`
   - JailCellGrid container: `flex-[7] h-full min-h-[50vh] lg:min-h-0`
   - Stats sidebar: `hidden lg:flex flex-[3] h-full overflow-y-auto bg-surface rounded-lg border border-border p-md`

5. Add BottomSheet at bottom of main, wrapping StatsPanel:
```tsx
<BottomSheet isOpen={isStatsOpen} onClose={() => setIsStatsOpen(false)}>
  <StatsPanel />
</BottomSheet>
```

Breakpoint mapping (per D-01 through D-04):
- `<768px (mobile)`: Stats hidden, button shows, bottom sheet for stats
- `768px-1023px (md:)`: Stats hidden, button shows, stacked layout (flex-col remains)
- `>=1024px (lg:)`: 70/30 sidebar layout, no button needed

The existing header structure needs ThemeToggle added. Keep the existing 'use client' directive and imports.
</action>
  <verify>
    <automated>grep -q "lg:flex-row" frontend/src/app/page.tsx && grep -q "lg:hidden" frontend/src/app/page.tsx && grep -q "BottomSheet" frontend/src/app/page.tsx && grep -q "ThemeToggle" frontend/src/app/page.tsx && grep -q "isStatsOpen" frontend/src/app/page.tsx</automated>
  </verify>
  <done>
    - page.tsx imports BottomSheet and ThemeToggle
    - useState controls bottom sheet visibility
    - Header contains ThemeToggle and mobile stats button
    - Main layout uses lg:flex-row for desktop, flex-col for mobile
    - Stats sidebar hidden on mobile/tablet (lg:flex)
    - BottomSheet wraps StatsPanel for mobile view
    - Mobile stats button shows only on lg:hidden
  </done>
</task>

</tasks>

<verification>
- `grep "lg:flex-row" frontend/src/app/page.tsx` returns responsive layout class
- `grep "lg:hidden" frontend/src/app/page.tsx` returns mobile button visibility
- `grep "BottomSheet" frontend/src/app/page.tsx` returns import
- `grep "export function BottomSheet" frontend/src/components/BottomSheet.tsx` returns component
- `grep "AnimatePresence" frontend/src/components/BottomSheet.tsx` returns animation import
</verification>

<success_criteria>
1. BottomSheet component with slide-up animation and drag-to-dismiss
2. page.tsx responsive at all three breakpoints (mobile, tablet, desktop)
3. ThemeToggle visible in header for all breakpoints
4. Mobile stats button shows stats in bottom sheet on mobile/tablet
5. Desktop keeps 70/30 sidebar layout unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish/04-02-SUMMARY.md`
</output>