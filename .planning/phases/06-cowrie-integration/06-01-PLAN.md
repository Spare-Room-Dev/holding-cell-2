---
phase: 06-cowrie-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docker-compose.yml, backend/requirements.txt, cowrie-config/motd, cowrie-config/userdb.txt, cowrie-config/honeyfs/etc/motd]
autonomous: true
requirements: [COW-01, COW-05]
user_setup:
  - service: MaxMind GeoLite2
    why: GeoIP country lookup from attacker IPs
    env_vars: []
    dashboard_config:
      - task: "Download GeoLite2-Country.mmdb"
        location: "https://dev.maxmind.com/geoip/geolite2-free-geolocation-data"
        instructions: "Create free MaxMind account, download GeoLite2-Country.mmdb, place in ./geoip/GeoLite2-Country.mmdb"

must_haves:
  truths:
    - "Cowrie container starts with mining/industrial OT persona (motd, usernames reflect OT environment)"
    - "GeoIP database is accessible to backend container for country lookups"
  artifacts:
    - path: "cowrie-config/motd"
      provides: "Pre-login banner for SSH"
      min_lines: 5
    - path: "cowrie-config/userdb.txt"
      provides: "OT usernames for honeypot"
      contains: "haulop"
    - path: "cowrie-config/honeyfs/etc/motd"
      provides: "Post-login banner"
      min_lines: 3
    - path: "docker-compose.yml"
      provides: "Volume mounts for Cowrie config and GeoIP"
      contains: "cowrie-config"
    - path: "backend/requirements.txt"
      provides: "Python dependencies for Cowrie integration"
      contains: "watchfiles"
      contains: "geoip2"
  key_links:
    - from: "docker-compose.yml"
      to: "cowrie-config/"
      via: "volume mount"
      pattern: "cowrie-config.*:.*:ro"
    - from: "docker-compose.yml"
      to: "./geoip/"
      via: "volume mount"
      pattern: "geoip.*:.*:ro"
---

<objective>
Configure Cowrie honeypot with mining/industrial OT persona and set up GeoIP database mount for country lookups.

Purpose: Transform generic Cowrie honeypot into a convincing OT/mining system persona that attracts realistic attacks, and enable GeoIP enrichment for attacker IP addresses.
Output: Cowrie configuration files, updated docker-compose.yml with volume mounts, backend requirements updated with watchfiles and geoip2.
</objective>

<execution_context>
@/Users/rob/Documents/OT Apps/Holding Cell 2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rob/Documents/OT Apps/Holding Cell 2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cowrie-integration/06-CONTEXT.md
@.planning/phases/06-cowrie-integration/06-RESEARCH.md
@docker-compose.yml
@backend/requirements.txt

<interfaces>
<!-- Key interfaces from existing codebase -->

From docker-compose.yml (current Cowrie config):
```yaml
cowrie:
  image: cowrie/cowrie:latest
  ports:
    - "${COWRIE_SSH_PORT:-2222}:2222"
    - "${COWRIE_TELNET_PORT:-2323}:2223"
  volumes:
    - cowrie-logs:/cowrie/cowrie-git/var/log/cowrie
  networks:
    - cowrie-network
```

From backend/requirements.txt (current dependencies):
```text
fastapi>=0.135.0
python-socketio[asyncio]>=5.9.0
uvicorn[standard]>=0.34.0
pydantic>=2.0.0
httpx>=0.28.0
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Cowrie OT Persona Configuration Files</name>
  <files>cowrie-config/motd, cowrie-config/userdb.txt, cowrie-config/honeyfs/etc/motd</files>
  <read_first>
    - .planning/phases/06-cowrie-integration/06-CONTEXT.md (D-01, D-03: mining persona requirements)
    - .planning/phases/06-cowrie-integration/06-RESEARCH.md (Cowrie configuration patterns)
  </read_first>
  <action>
    Create the cowrie-config directory structure with OT/mining persona files:

    1. Create `cowrie-config/motd` (pre-login SSH banner):
       ```
       Welcome to HaulMax Fleet Management System v2.4.1
       System Status: OPERATIONAL
       Last Sync: [timestamp]
       Active Units: 12 haul trucks, 3 loaders
       Current Shift: Day Operations

       Type 'help' for available commands.
       ```

    2. Create `cowrie-config/userdb.txt` with OT usernames (per D-03):
       ```
       # Mining/Industrial OT usernames
       # Format: username:uid:password
       # * = wildcard password (accepts anything)
       # ! = deny this password

       root:x:!root
       root:x:!123456
       root:x:!password
       root:x:*
       haulop:x:*
       dispatch:x:*
       supervisor:x:*
       mechanic:x:*
       service:x:*
       operator:x:*
       admin:x:*
       ```

    3. Create `cowrie-config/honeyfs/etc/motd` (post-login banner):
       ```
       HaulMax Fleet Management System v2.4.1
       Copyright (C) 2024 Mining Technologies Inc.

       Authorized personnel only. All activity is logged.
       ```

    Per D-01: Mining automation system persona with haul truck telemetry, OT environment paths, and industrial usernames.
    Per D-03: Configuration files for motd, fs (filesystem), and userdb.txt.
  </action>
  <verify>
    <automated>test -f cowrie-config/motd && test -f cowrie-config/userdb.txt && test -f cowrie-config/honeyfs/etc/motd && grep -q "haulop" cowrie-config/userdb.txt && echo "PASS: Cowrie config files created"</automated>
  </verify>
  <done>
    - cowrie-config/motd exists with mining system welcome banner
    - cowrie-config/userdb.txt exists with OT usernames (haulop, dispatch, supervisor, etc.)
    - cowrie-config/honeyfs/etc/motd exists with post-login banner
  </done>
</task>

<task type="auto">
  <name>Task 2: Update docker-compose.yml with Cowrie Config and GeoIP Volume Mounts</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (current Cowrie service config)
    - .planning/phases/06-cowrie-integration/06-CONTEXT.md (D-02, D-04, D-06, D-21)
    - .planning/phases/06-cowrie-integration/06-RESEARCH.md (volume mount patterns)
  </read_first>
  <action>
    Update docker-compose.yml to mount Cowrie configuration files and GeoIP database:

    1. In the `cowrie` service, add volume mounts:
       ```yaml
       volumes:
         - cowrie-logs:/cowrie/cowrie-git/var/log/cowrie
         - ./cowrie-config/userdb.txt:/cowrie/cowrie-git/etc/userdb.txt:ro
         - ./cowrie-config/honeyfs:/cowrie/cowrie-git/honeyfs:ro
       ```

    2. In the `backend` service, add GeoIP volume mount:
       ```yaml
       volumes:
         - cowrie-logs:${COWRIE_LOG_PATH:-/var/log/cowrie}:ro
         - ./geoip:/geoip:ro
       ```

    3. Add environment variable for GeoIP path in backend:
       ```yaml
       environment:
         - COWRIE_LOG_PATH=${COWRIE_LOG_PATH:-/var/log/cowrie}
         - GEOIP_DB_PATH=${GEOIP_DB_PATH:-/geoip/GeoLite2-Country.mmdb}
       ```

    Per D-02: Use official cowrie/cowrie:latest image with volume-mounted config files.
    Per D-04: Cowrie SSH on port 2222, Telnet on port 2323 (already configured).
    Per D-21: GeoIP database mounted as Docker volume to backend container.
  </action>
  <verify>
    <automated>grep -q "cowrie-config" docker-compose.yml && grep -q "geoip" docker-compose.yml && grep -q "GEOIP_DB_PATH" docker-compose.yml && echo "PASS: docker-compose.yml updated with volume mounts"</automated>
  </verify>
  <done>
    - docker-compose.yml has cowrie-config volume mounts for Cowrie service
    - docker-compose.yml has ./geoip volume mount for backend service
    - GEOIP_DB_PATH environment variable added to backend
  </done>
</task>

<task type="auto">
  <name>Task 3: Add watchfiles and geoip2 to Backend Requirements</name>
  <files>backend/requirements.txt</files>
  <read_first>
    - backend/requirements.txt (current dependencies)
    - .planning/phases/06-cowrie-integration/06-RESEARCH.md (Standard Stack section)
  </read_first>
  <action>
    Add the required Python dependencies for Cowrie integration:

    Append to backend/requirements.txt:
    ```
    watchfiles>=1.1.1
    geoip2>=5.2.0
    ```

    Per D-05: Async file tailing with Python watchdog or aiofiles — RESEARCH.md recommends watchfiles v1.1.1 for native async support.
    Per D-20: MaxMind GeoLite2 database — RESEARCH.md recommends geoip2 v5.2.0 (official MaxMind Python library).
  </action>
  <verify>
    <automated>grep -q "watchfiles>=1.1.1" backend/requirements.txt && grep -q "geoip2>=5.2.0" backend/requirements.txt && echo "PASS: Requirements updated"</automated>
  </verify>
  <done>
    - backend/requirements.txt contains watchfiles>=1.1.1
    - backend/requirements.txt contains geoip2>=5.2.0
  </done>
</task>

</tasks>

<verification>
- [ ] Cowrie configuration files created in cowrie-config/
- [ ] docker-compose.yml updated with volume mounts for Cowrie config and GeoIP
- [ ] backend/requirements.txt updated with watchfiles and geoip2
- [ ] All file paths use correct Docker container paths
</verification>

<success_criteria>
- Cowrie container can start with OT persona files mounted
- Backend container has GeoIP volume mount ready for country lookups
- Dependencies added for async log watching and GeoIP lookups
</success_criteria>

<output>
After completion, create `.planning/phases/06-cowrie-integration/06-01-SUMMARY.md`
</output>