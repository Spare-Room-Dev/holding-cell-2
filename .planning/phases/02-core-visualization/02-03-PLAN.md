---
phase: 02-core-visualization
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - frontend/src/app/page.tsx
autonomous: false
requirements: []
user_setup: []
must_haves:
  truths:
    - "User sees jail cell on the left (~70% width) and stats panel on the right (~30% width)"
    - "User sees jail cell fills full viewport height minus header"
    - "User sees stats panel scrolls independently if content overflows"
    - "User sees real-time attacks in the cell and counters updating"
  artifacts:
    - path: "frontend/src/app/page.tsx"
      provides: "Dashboard layout with JailCellGrid and StatsPanel"
      contains: "JailCellGrid"
      contains: "StatsPanel"
  key_links:
    - from: "page.tsx"
      to: "JailCellGrid.tsx"
      via: "import and JSX"
    - from: "page.tsx"
      to: "StatsPanel.tsx"
      via: "import and JSX"
---

<objective>
Integrate JailCellGrid and StatsPanel into the dashboard with 70/30 sidebar layout.

Purpose: Replace placeholder components with real visualization components, implementing the sidebar layout per D-01.
Output: Updated page.tsx with JailCellGrid and StatsPanel in correct layout
</objective>

<execution_context>
@/Users/rob/Documents/OT Apps/Holding Cell 2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rob/Documents/OT Apps/Holding Cell 2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-visualization/02-CONTEXT.md
@.planning/phases/02-core-visualization/02-UI-SPEC.md
@DESIGN.md

<!-- Components to integrate -->
@frontend/src/components/JailCellGrid.tsx
@frontend/src/components/StatsPanel.tsx

<!-- Current page structure -->
@frontend/src/app/page.tsx
</context>

<interfaces>
<!-- Key interfaces executor needs. Extracted from components. -->

From frontend/src/components/JailCellGrid.tsx:
```typescript
export function JailCellGrid(): JSX.Element
// Renders jail cell with stone texture, iron bars, and prisoner stack
// No props required - uses useSocket() internally
```

From frontend/src/components/StatsPanel.tsx:
```typescript
export function StatsPanel(): JSX.Element
// Renders horizontal row of LED counters
// No props required - uses useSocket() internally
```

From frontend/src/app/page.tsx (current structure):
```typescript
// Current: Placeholder divs for Phase 2 components
// Lines 45-64: JailCellGrid placeholder
// Lines 66-71: StatsPanel placeholder
```

From DESIGN.md (layout):
```markdown
- **Layout:** Grid-disciplined shell with creative focal point
- **Grid:** 12-column grid on desktop
- **Max content width:** 1200px
```
</interfaces>

<tasks>

<task type="auto">
<name>Task 1: Update page.tsx with sidebar layout and real components</name>
<files>frontend/src/app/page.tsx</files>
<read_first>
- frontend/src/app/page.tsx (current placeholder structure to replace)
- frontend/src/components/JailCellGrid.tsx (component to import)
- frontend/src/components/StatsPanel.tsx (component to import)
- .planning/phases/02-core-visualization/02-CONTEXT.md (D-01: 70/30 sidebar layout, D-02: full viewport height, D-03: independent scroll)
</read_first>
<acceptance_criteria>
- page.tsx imports JailCellGrid from '@/components/JailCellGrid'
- page.tsx imports StatsPanel from '@/components/StatsPanel'
- Layout uses flex-[7] for JailCellGrid (70% width)
- Layout uses flex-[3] for StatsPanel (30% width)
- StatsPanel container has overflow-y-auto class
- Placeholder content removed
- ARCHETYPE_COLORS and getArchetypeColor removed (moved to PrisonerSlot)
- Connection status paragraph removed (ConnectionStatus component handles display)
</acceptance_criteria>
<behavior>
- Header remains unchanged (title + ConnectionStatus)
- Main content uses flexbox with 70/30 split
- JailCellGrid fills remaining viewport height minus header
- StatsPanel scrolls independently
- Remove placeholder content
- Remove temporary attack display (no longer needed)
</behavior>
<action>
Update frontend/src/app/page.tsx:

1. Update imports:
   - Remove ARCHETYPE_COLORS constant (no longer needed in page.tsx)
   - Remove getArchetypeColor function (no longer needed)
   - Add: import { JailCellGrid } from '@/components/JailCellGrid'
   - Add: import { StatsPanel } from '@/components/StatsPanel'
   - Keep: ConnectionStatus import

2. Keep header unchanged:
   - "Holding Cell" title
   - ConnectionStatus component

3. Replace main content area (lines ~35-72):
   - Remove placeholder divs for JailCellGrid and StatsPanel
   - Remove temporary attack display (no longer needed)
   - Remove ARCHETYPE_COLORS constant (moved to PrisonerSlot)
   - Remove useSocket() hook call (no longer needed in page.tsx)

4. Implement sidebar layout (per D-01, D-02, D-03):
   - Container: div with flex flex-row gap-md, full height
   - Height calculation: h-[calc(100vh-5rem)] to fill viewport minus header (header is ~4rem + padding)
   - JailCellGrid container:
     * flex-[7] (70% width)
     * h-full to fill parent height
     * overflow-hidden
   - StatsPanel container:
     * flex-[3] (30% width)
     * overflow-y-auto (independent scroll per D-03)
     * bg-surface, rounded-lg, border border-border, p-md

5. Layout structure:
   ```tsx
   <main className="min-h-screen bg-background text-text-primary">
     {/* Header */}
     <header className="flex items-center justify-between px-lg py-md border-b border-border">
       <h1 className="font-display text-h1 text-text-primary">
         Holding Cell
       </h1>
       <ConnectionStatus />
     </header>

     {/* Main content - 70/30 sidebar layout */}
     <div className="p-lg h-[calc(100vh-5rem)]">
       <div className="flex flex-row gap-md h-full">
         {/* Jail Cell - 70% width */}
         <div className="flex-[7] h-full">
           <JailCellGrid />
         </div>
         {/* Stats Panel - 30% width */}
         <div className="flex-[3] h-full overflow-y-auto bg-surface rounded-lg border border-border p-md">
           <StatsPanel />
         </div>
       </div>
     </div>
   </main>
   ```

6. Remove useSocket() hook from Dashboard component if no longer needed after changes

Note: The header height is approximately py-md (16px * 2) + h1 height (~24px) + border-b = ~56px. Using h-[calc(100vh-5rem)] = 100vh - 80px provides safe spacing.
</action>
<verify>
<automated>grep -c "JailCellGrid" frontend/src/app/page.tsx && grep -c "StatsPanel" frontend/src/app/page.tsx && grep -c "flex-\\[7\\]" frontend/src/app/page.tsx && grep -c "flex-\\[3\\]" frontend/src/app/page.tsx</automated>
</verify>
<done>
- page.tsx imports JailCellGrid and StatsPanel components
- Layout implements 70/30 sidebar split (flex-[7] / flex-[3])
- JailCellGrid fills full height
- StatsPanel scrolls independently
- Placeholder content removed
- Build succeeds with no TypeScript errors
</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
<name>Task 2: Verify dashboard integration visually</name>
<files>N/A - human verification checkpoint</files>
<action>Human verification checkpoint - no code changes. Review the dashboard visually to confirm the integration is correct.</action>
<what-built>Updated dashboard with JailCellGrid (stone texture, iron bars, prisoner placeholders) and StatsPanel (LED counters) in 70/30 sidebar layout.</what-built>
<how-to-verify>
1. Start both servers: `npm run dev:all` from project root
2. Open browser to http://localhost:3000
3. Verify:
   - Header shows "Holding Cell" title and LIVE badge (green glow)
   - Main area shows ~70% jail cell on left with stone/brick texture
   - Iron bars overlay visible on cell
   - ~30% stats panel on right with 6 counters in horizontal row
   - Counters show: Total, Script Kiddies, APT Operatives, Botnet Drones, IoT Worms, Hacktivists
   - Cell shows "The cell is empty. Waiting for attackers..." when no attacks
   - As attacks arrive (every 3-8 seconds from backend):
     * Prisoner boxes appear in cell, stacked from bottom
     * Counters increment with phosphor green glow
     * Maximum 20 prisoners visible at once
     * Older prisoners fade out when count exceeds 20
</how-to-verify>
<verify>
<automated>N/A - human verification checkpoint</automated>
</verify>
<done>User confirms dashboard displays correctly with all components visible and functioning.</done>
<resume-signal>Type "approved" to continue, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
## Overall Phase Checks

### Layout Structure
```bash
grep -c "JailCellGrid" frontend/src/app/page.tsx
# Expected: >= 1 (component used)
grep -c "StatsPanel" frontend/src/app/page.tsx
# Expected: >= 1 (component used)
grep "flex-\\[7\\]" frontend/src/app/page.tsx
# Expected: 70% width for JailCellGrid
grep "flex-\\[3\\]" frontend/src/app/page.tsx
# Expected: 30% width for StatsPanel
```

### Independent Scroll
```bash
grep "overflow-y-auto" frontend/src/app/page.tsx
# Expected: StatsPanel container has independent scroll
```

### Placeholders Removed
```bash
grep "Jail Cell Grid will be implemented" frontend/src/app/page.tsx
# Expected: 0 (placeholder removed)
grep "Stats Panel will be implemented" frontend/src/app/page.tsx
# Expected: 0 (placeholder removed)
```

## Build Verification
```bash
cd frontend && npm run build
# Expected: Build succeeds without errors
```

## End-to-End Verification
```bash
# Start backend (terminal 1)
cd backend && python main.py &

# Start frontend (terminal 2)
cd frontend && npm run dev &

# Or use root script
npm run dev:all
```

## Visual Verification (Manual)
1. Open http://localhost:3000
2. Verify 70/30 sidebar layout
3. Verify stone texture background on cell
4. Verify iron bar overlay
5. Verify LED counter aesthetic on stats
6. Verify attacks appear in cell and update counters
</verification>

<success_criteria>
- Dashboard shows JailCellGrid on left (~70% width)
- Dashboard shows StatsPanel on right (~30% width)
- JailCellGrid fills viewport minus header
- StatsPanel scrolls independently
- Real-time attacks appear in cell and update counters
- Build succeeds with no TypeScript errors
- Manual verification passes all checks
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-visualization/02-03-SUMMARY.md`
</output>