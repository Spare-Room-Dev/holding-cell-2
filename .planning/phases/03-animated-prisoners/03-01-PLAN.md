---
phase: 03-animated-prisoners
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/PrisonerSprite.tsx
  - frontend/src/components/ArrestRecordTooltip.tsx
autonomous: true
requirements: [PRSN-01, PRSN-02, PRSN-03, TOOL-01, TOOL-02, TOOL-03]
user_setup: []

must_haves:
  truths:
    - "User sees pixel-art prisoner sprites for each archetype (not placeholder boxes)"
    - "Each archetype has distinct bandana color matching ARCHETYPE_COLORS"
    - "Sprites render at 56x56 with pixelated scaling"
    - "Hover shows ArrestRecord tooltip after 150ms delay"
    - "Tooltip displays IP, country flag emoji, port/protocol, archetype badge, commands count, duration, time arrested"
    - "Tooltip has terminal aesthetic: background #1A1A1A, phosphor border, IBM Plex Mono font"
  artifacts:
    - path: "frontend/src/components/PrisonerSprite.tsx"
      provides: "Inline SVG sprites for 5 archetypes"
      exports: ["PrisonerSprite"]
      contains: "BANDANA_COLORS mapping"
    - path: "frontend/src/components/ArrestRecordTooltip.tsx"
      provides: "Hover tooltip component with terminal styling"
      exports: ["ArrestRecordTooltip"]
      contains: "formatTimeAgo, formatDuration helpers"
  key_links:
    - from: "PrisonerSprite.tsx"
      to: "ARCHETYPE_COLORS"
      via: "BANDANA_COLORS mapping"
      pattern: "script_kiddie.*#FFB800"
    - from: "ArrestRecordTooltip.tsx"
      to: "AttackEvent type"
      via: "attack prop"
      pattern: "attack\\.(ip|country|countryCode|port|protocol)"
---

<objective>
Create pixel-art prisoner sprites as inline SVGs and an interactive tooltip component with terminal styling.

Purpose: Replace Phase 2 placeholder boxes with actual pixel-art sprites, providing visual archetype identification. The tooltip reveals detailed attack information on hover.

Output: Two new components that will be integrated into PrisonerSlot in the next plan.
</objective>

<execution_context>
@/Users/rob/Documents/OT Apps/Holding Cell 2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rob/Documents/OT Apps/Holding Cell 2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-animated-prisoners/03-CONTEXT.md
@.planning/phases/03-animated-prisoners/03-RESEARCH.md
@.planning/phases/03-animated-prisoners/03-UI-SPEC.md

<!-- Existing component patterns -->
@frontend/src/components/PrisonerSlot.tsx
@frontend/src/types/attack.ts
@frontend/src/app/globals.css

<interfaces>
<!-- Key types from attack.ts -->
```typescript
export type Archetype =
  | 'script_kiddie'
  | 'botnet_drone'
  | 'apt_operative'
  | 'iot_worm'
  | 'hacktivist';

export interface AttackEvent {
  id: string;
  timestamp: string;
  ip: string;
  country: string;
  countryCode: string;
  port: number;
  protocol: string;
  archetype: Archetype;
  commands: string[];
  duration: number;
  rawLog: string;
}
```

<!-- ARCHETYPE_COLORS pattern from PrisonerSlot.tsx -->
```typescript
const ARCHETYPE_COLORS: Record<Archetype, string> = {
  script_kiddie: 'amber',
  botnet_drone: 'phosphor',
  apt_operative: 'alert',
  iot_worm: 'purple-400',
  hacktivist: 'blue-400',
};
```

<!-- Color values from globals.css -->
- Phosphor: #00FF88
- Amber: #FFB800
- Alert: #FF3B5C
- Purple-400: #A855F7 (Tailwind default)
- Blue-400: #60A5FA (Tailwind default)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PrisonerSprite component with inline SVG sprites</name>
  <files>frontend/src/components/PrisonerSprite.tsx</files>
  <read_first>
    - frontend/src/components/PrisonerSlot.tsx (for ARCHETYPE_COLORS pattern and component structure)
    - frontend/src/types/attack.ts (for Archetype type)
    - frontend/src/app/globals.css (for color variables)
  </read_first>
  <action>
Create `frontend/src/components/PrisonerSprite.tsx` that exports a React component rendering pixel-art prisoner sprites as inline SVG.

Per D-14: Inline SVG sprites per archetype (no external image files).
Per D-15: Bandana colors match ARCHETYPE_COLORS mapping.

Implementation details:
1. Create `BANDANA_COLORS` object mapping each archetype to hex colors:
   - `script_kiddie: '#FFB800'` (amber)
   - `botnet_drone: '#00FF88'` (phosphor)
   - `apt_operative: '#FF3B5C'` (alert)
   - `iot_worm: '#A855F7'` (purple-400)
   - `hacktivist: '#60A5FA'` (blue-400)

2. Create a 32x32 pixel-art SVG with these elements:
   - Body: White/gray pixels (fill: #F0F0F0) for torso (rect x="12" y="16" width="8" height="12")
   - Head: White/gray pixels (fill: #F0F0F0) for head (rect x="10" y="6" width="12" height="10")
   - Bandana: Colored by archetype (dynamic fill from BANDANA_COLORS) at rect x="10" y="4" width="12" height="4"
   - Eyes: Black pixels (fill: #0D0D0D) as two 2x2 rects at x="12" y="10" and x="18" y="10"
   - Legs: Dark pixels (fill: #1A1A1A) as two 4x4 rects at bottom

3. SVG rendering specs (per D-14):
   - viewBox="0 0 32 32"
   - width="56" height="56" (display size, not 64x64 per RESEARCH correction)
   - style={{ imageRendering: 'pixelated' }}
   - Add className="pixel-sprite" for potential CSS targeting
   - Wrap in 56x56 container (w-14 h-14 in Tailwind)

4. Component props:
   - `archetype: Archetype` (required)
   - `className?: string` (optional)

5. Export as named export `PrisonerSprite`.

Do NOT:
- Create separate sprite files — all 5 variants inline in one component
- Add external image dependencies
- Use different display sizes than 56x56
- Forget to handle all 5 archetypes with the same base sprite structure
  </action>
  <verify>
    <automated>grep -q "export function PrisonerSprite" frontend/src/components/PrisonerSprite.tsx && grep -q "BANDANA_COLORS" frontend/src/components/PrisonerSprite.tsx && grep -q "viewBox=\"0 0 32 32\"" frontend/src/components/PrisonerSprite.tsx && grep -q "width=\"56\"" frontend/src/components/PrisonerSprite.tsx && grep -q "imageRendering.*pixelated" frontend/src/components/PrisonerSprite.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/PrisonerSprite.tsx` exists
    - File contains `export function PrisonerSprite` function
    - File contains `BANDANA_COLORS` mapping with all 5 archetypes
    - SVG has viewBox="0 0 32 32" for 32x32 pixel-art
    - SVG renders at width="56" height="56" for display
    - SVG has `imageRendering: 'pixelated'` style
    - Component accepts `archetype: Archetype` prop
    - Bandana fill uses `BANDANA_COLORS[archetype]` dynamically
  </acceptance_criteria>
  <done>PrisonerSprite component renders 56x56 pixel-art sprites with archetype-colored bandanas. All 5 archetypes supported with identical base sprite structure. Ready for integration into PrisonerSlot.</done>
</task>

<task type="auto">
  <name>Task 2: Create ArrestRecordTooltip component with terminal styling</name>
  <files>frontend/src/components/ArrestRecordTooltip.tsx</files>
  <read_first>
    - frontend/src/types/attack.ts (for AttackEvent interface)
    - frontend/src/app/globals.css (for color variables and font classes)
    - .planning/phases/03-animated-prisoners/03-CONTEXT.md (for D-16 tooltip specs)
    - .planning/phases/03-animated-prisoners/03-UI-SPEC.md (for tooltip content and styling)
  </read_first>
  <action>
Create `frontend/src/components/ArrestRecordTooltip.tsx` that exports a React component displaying arrest record details on hover.

Per D-16: Terminal aesthetic with background #1A1A1A, phosphor border, IBM Plex Mono font, 280px fixed width.

Implementation details:
1. Create helper functions:
   - `formatTimeAgo(timestamp: string): string` — converts ISO timestamp to relative time ("just now", "5m ago", "2h ago", "1d ago")
   - `formatDuration(seconds: number): string` — converts duration to "Xm Xs" format
   - `countryCodeToFlag(countryCode: string): string` — converts country code to flag emoji using the formula: `String.fromCodePoint(...countryCode.toUpperCase().split('').map(c => 127397 + c.charCodeAt(0)))`

2. Component props:
   - `attack: AttackEvent` (required)

3. Tooltip content (per TOOL-01):
   - Archetype badge as colored pill with uppercase text (e.g., "SCRIPT KIDDIE")
   - IP address in phosphor color (#00FF88)
   - Country flag emoji + country name
   - Protocol/Port (e.g., "SSH/22")
   - Commands count (e.g., "Commands: 15")
   - Duration (e.g., "Duration: 3m 42s")
   - Time arrested (relative time)

4. Styling (per D-16):
   - Container: `position: absolute`, `top: -8px`, `left: 50%`, `transform: translateX(-50%)`
   - Width: fixed 280px
   - Background: #1A1A1A (var(--color-surface))
   - Border: 1px solid #00FF88 (var(--color-phosphor))
   - Border-radius: 8px
   - Padding: 12px
   - Font: IBM Plex Mono 14px (font-mono class)
   - Z-index: 50 (above iron bar overlay)

5. Archetype badge styling:
   - Background: `var(--color-phosphor-glow)` (rgba(0, 255, 136, 0.15))
   - Text color: phosphor (#00FF88)
   - Padding: 2px 8px
   - Border-radius: 4px
   - Uppercase, font-size: 12px

6. Text colors:
   - Primary values (IP, port/protocol): phosphor (#00FF88)
   - Labels and secondary text: muted (#6B6B6B)

7. Export as named export `ArrestRecordTooltip`.

Do NOT:
- Install external packages for flag emoji — use the manual calculation formula
- Position tooltip below the avatar — must be above
- Use different width than 280px
- Forget to handle edge cases for countryCode (empty string, invalid codes)
  </action>
  <verify>
    <automated>grep -q "export function ArrestRecordTooltip" frontend/src/components/ArrestRecordTooltip.tsx && grep -q "formatTimeAgo" frontend/src/components/ArrestRecordTooltip.tsx && grep -q "formatDuration" frontend/src/components/ArrestRecordTooltip.tsx && grep -q "countryCodeToFlag" frontend/src/components/ArrestRecordTooltip.tsx && grep -q "w-\\[280px\\]" frontend/src/components/ArrestRecordTooltip.tsx && grep -q "bg-\\[#1A1A1A\\]" frontend/src/components/ArrestRecordTooltip.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/ArrestRecordTooltip.tsx` exists
    - File contains `export function ArrestRecordTooltip` function
    - File contains `formatTimeAgo` helper function
    - File contains `formatDuration` helper function
    - File contains `countryCodeToFlag` helper function using `String.fromCodePoint`
    - Tooltip has fixed width of 280px (w-[280px])
    - Tooltip has background color #1A1A1A
    - Tooltip has phosphor border (#00FF88)
    - Component accepts `attack: AttackEvent` prop
    - Tooltip displays: archetype badge, IP, country flag + name, protocol/port, commands count, duration, time arrested
  </acceptance_criteria>
  <done>ArrestRecordTooltip component displays attack details with terminal aesthetic. Converts country codes to flag emojis without external dependencies. Ready for integration with hover trigger in PrisonerSlot.</done>
</task>

</tasks>

<verification>
- [ ] PrisonerSprite.tsx exports component with archetype prop
- [ ] PrisonerSprite renders 32x32 pixel-art SVG scaled to 56x56
- [ ] PrisonerSprite applies correct bandana color per archetype
- [ ] ArrestRecordTooltip.tsx exports component with attack prop
- [ ] ArrestRecordTooltip displays all required fields
- [ ] ArrestRecordTooltip uses terminal aesthetic styling
- [ ] No external dependencies installed (manual flag emoji calculation)
</verification>

<success_criteria>
Both components compile without errors. PrisonerSprite renders identical base sprites with archetype-colored bandanas. ArrestRecordTooltip displays formatted attack details with terminal aesthetic. Both ready for PrisonerSlot integration.
</success_criteria>

<output>
After completion, create `.planning/phases/03-animated-prisoners/03-01-SUMMARY.md`
</output>